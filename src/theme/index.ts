import { colors, spacing, fontSizes, typography, radius, shadows, opacity, interaction } from './tokens';

export const theme = {
  colors,
  spacing,
  fontSizes,
  typography,
  radius,
  shadows,
  opacity,
  interaction,
};

export type Theme = typeof theme;

